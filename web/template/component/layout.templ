package component

templ DefaultLayout(title string) {
	@htmlTemplate(title) {
		<div class="default-layout">
			<a href="/">
				@MainHeading(title)
			</a>
			{ children... }
		</div>
	}
}
