package page

import "github.com/qxuken/short/web/template/component"

templ Index(devMode bool, baseUrl, url, short string) {
	@component.Layout(devMode, "ShortiQ") {
		@component.CreateLink(baseUrl, url, short)
	}
}
