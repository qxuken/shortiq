package page

import "github.com/qxuken/short/web/template/component"

templ Stats(short templ.SafeURL, notification string) {
	@component.DefaultLayout("SHORTIQ") {
		@component.LinkStats(short, notification)
	}
}
